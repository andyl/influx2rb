#!/usr/bin/env ruby

# This script simply loads the `influx2rb` classes and starts an IRB session.

require 'irb'
IRBCFG = File.expand_path('~/.irbrc')
load IRBCFG if File.exist?(IRBCFG)

require 'bundler/setup'
require 'influx2rb'

system("clear")

IRB.start
